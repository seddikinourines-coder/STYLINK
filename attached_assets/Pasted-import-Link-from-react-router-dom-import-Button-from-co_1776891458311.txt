import { Link } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { ArrowRight } from "lucide-react";
import heroImage from "@/assets/hero-fashion.jpg";
import { mockProducts, mockDesigners } from "@/data/mockData";
import Layout from "@/components/layout/Layout";
import ProductCard from "@/components/ProductCard";
import FashionMap from "@/components/FashionMap";

const Index = () => {
  return (
    <Layout>

      {/* Hero */}
      <section className="relative h-[85vh] overflow-hidden">
        <img src={heroImage} className="absolute inset-0 w-full h-full object-cover" />
        <div className="absolute inset-0 bg-gradient-to-r from-black/60 to-transparent" />

        <div className="relative container h-full flex items-center">
          <div className="max-w-lg text-white">
            <h1 className="text-5xl font-serif mb-4">
              Where Fashion Meets Craftsmanship
            </h1>
            <p className="mb-6">
              Discover curated collections from independent designers.
            </p>

            <div className="flex gap-4">
              <Link to="/shop">
                <Button>Shop Now</Button>
              </Link>

              <Link to="/designers">
                <Button variant="outline">Meet Designers</Button>
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* Products */}
      <section className="container py-20">
        <h2 className="text-3xl mb-8">New Arrivals</h2>

        <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
          {mockProducts.slice(0, 4).map((p) => (
            <ProductCard key={p.id} product={p} />
          ))}
        </div>
      </section>

      {/* Designers */}
      <section className="bg-gray-50 py-20">
        <div className="container">
          <h2 className="text-3xl mb-8">Featured Designers</h2>

          <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
            {mockDesigners.map((d) => (
              <Link key={d.id} to={`/designers/${d.id}`}>
                <img src={d.image} className="rounded-lg mb-2" />
                <p>{d.name}</p>
              </Link>
            ))}
          </div>
        </div>
      </section>

      <FashionMap />

    </Layout>
  );
};

export default Index;